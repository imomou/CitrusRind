---
platform: linux

image_resource:
  type: docker-image
  source: {repository: golang, tag: alpine}

run:
  path: /bin/sh
  args:
    - -c
    - apk add git zip
    
  outputs:
    - name: result-image